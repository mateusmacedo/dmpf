// Package domainkit runs a UPR by value and observes its outcome as a
// Projection (KIT-01) with no test double inside the domain: it imports
// dmpf-domain only and never testing, os, time or encoding/json. Domain block.
package domainkit
