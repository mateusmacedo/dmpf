// Package tb is the only place the kit touches testing, os and the repository
// tree: it turns a Verdict into a test failure, reads fixtures from contracts/
// and resolves the environment a harness needs. App block.
package tb
