// Package ports is the port block of the resource-scheduling bounded
// context: the declared boundary to state, outbox and inbox, with no realization here.
// Its unit and its bounded context are stated in dmpf-units.json (ADR-012).
package ports
