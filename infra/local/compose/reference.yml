# Topologia de referência (ADR-044): o BFF é a única borda HTTP publicada no
# host; orders, reservations e bookings servem gRPC só na rede do projeto, cada um com o
# próprio banco e role, e cada relay drena apenas a outbox do seu contexto. Os
# bancos nascem no postgres-init; o schema, no papel api (MIGRATE=true). Relay e
# consumer reiniciam até as tabelas existirem. Os tópicos vêm do redpanda-init.
x-app: &app
  profiles: [dmpf]
  restart: on-failure

x-orders-image: &orders-image
  build:
    context: ../../..
    dockerfile: apps/backend/orders/Dockerfile
  image: ${ORDERS_IMAGE:-orders:local}

x-reservations-image: &reservations-image
  build:
    context: ../../..
    dockerfile: apps/backend/reservations/Dockerfile
  image: ${RESERVATIONS_IMAGE:-reservations:local}

x-bookings-image: &bookings-image
  build:
    context: ../../..
    dockerfile: apps/backend/bookings/Dockerfile
  image: ${BOOKINGS_IMAGE:-bookings:local}

x-dmpf-env: &dmpf-env
  OTLP_ENDPOINT: otel-collector:4317
  OTLP_INSECURE: 'true'
  SERVICE_VERSION: ${SERVICE_VERSION:-local}

# Sem TLS no broker local (opt-out de desenvolvimento), mas com o cliente
# autenticado por SASL SCRAM no listener interno: cada processo tem o próprio
# principal, criado no redpanda-init (IDN-04).
x-kafka-env: &kafka-env
  KAFKA_BROKERS: redpanda:29092
  KAFKA_INSECURE: 'true'
  KAFKA_GROUP: ${KAFKA_GROUP:-reservations}
  KAFKA_SASL_MECHANISM: SCRAM-SHA-256

x-kafka-orders: &kafka-orders
  KAFKA_SASL_USERNAME: orders
  KAFKA_SASL_PASSWORD: ${KAFKA_ORDERS_PASSWORD:-orders-local}

x-kafka-reservations: &kafka-reservations
  KAFKA_SASL_USERNAME: reservations
  KAFKA_SASL_PASSWORD: ${KAFKA_RESERVATIONS_PASSWORD:-reservations-local}

x-kafka-bookings: &kafka-bookings
  KAFKA_SASL_USERNAME: bookings
  KAFKA_SASL_PASSWORD: ${KAFKA_BOOKINGS_PASSWORD:-bookings-local}

x-orders-env: &orders-env
  SERVICE: orders
  PG_DSN: postgres://orders:${ORDERS_PG_PASSWORD:-orders-local}@postgres:5432/orders?sslmode=disable

x-reservations-env: &reservations-env
  SERVICE: reservations
  PG_DSN: postgres://reservations:${RESERVATIONS_PG_PASSWORD:-reservations-local}@postgres:5432/reservations?sslmode=disable

x-bookings-env: &bookings-env
  SERVICE: bookings
  PG_DSN: postgres://bookings:${BOOKINGS_PG_PASSWORD:-bookings-local}@postgres:5432/bookings?sslmode=disable

# gRPC interno com mTLS (IDN-03): cada api apresenta o próprio certificado e só
# serve o workload que a CA local assinou e a allowlist nomeia — o BFF. Os
# arquivos vêm do volume do pki-init.
x-grpc-server-env: &grpc-server-env
  GRPC_ADDR: ':9090'
  GRPC_CLIENT_CA_FILE: /pki/ca.crt
  GRPC_TRUSTED_CLIENTS: spiffe://dmpf/bff
  MIGRATE: 'true'

x-pki: &pki
  - dmpf-pki:/pki:ro

x-app-limits: &app-limits
  resources:
    limits: { cpus: '0.15', memory: 192M }
    reservations: { cpus: '0.03', memory: 32M }

x-worker-limits: &worker-limits
  resources:
    limits: { cpus: '0.10', memory: 128M }
    reservations: { cpus: '0.02', memory: 32M }

services:
  postgres-init:
    image: postgres:16-alpine
    profiles: [dmpf]
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGHOST: postgres
      PGUSER: ${POSTGRES_USER:-postgres}
      PGPASSWORD: ${POSTGRES_PASSWORD:-postgres}
      PGDATABASE: postgres
    entrypoint: ['/bin/sh', '-c']
    # Idempotente: cria o role e o banco que ainda não existem e reconcilia a
    # senha do role a cada execução, para que trocar a senha com o volume mantido
    # não deixe a app sem conexão. O banco pertence ao role da app, que assim cria
    # o próprio schema sem privilégio administrativo.
    command:
      - >-
        set -e; for pair in orders:${ORDERS_PG_PASSWORD:-orders-local}
        reservations:${RESERVATIONS_PG_PASSWORD:-reservations-local}
        bookings:${BOOKINGS_PG_PASSWORD:-bookings-local}; do
        app=$${pair%%:*}; pass=$${pair#*:};
        if ! psql -tAc "SELECT 1 FROM pg_roles WHERE rolname = '$$app'" | grep -q 1; then
        psql -c "CREATE ROLE \"$$app\" LOGIN"; fi;
        psql -c "ALTER ROLE \"$$app\" PASSWORD '$$pass'";
        if ! psql -tAc "SELECT 1 FROM pg_database WHERE datname = '$$app'" | grep -q 1; then
        psql -c "CREATE DATABASE \"$$app\" OWNER \"$$app\""; fi; done
    deploy:
      resources:
        limits: { cpus: '0.05', memory: 32M }
        reservations: { cpus: '0.01', memory: 16M }

  redpanda-init:
    image: redpandadata/redpanda:v26.2.2
    profiles: [dmpf]
    depends_on:
      redpanda:
        condition: service_healthy
    entrypoint: ['/bin/sh', '-c']
    # A ligação entre principal e source que o consumer admite é a ACL (ADR-052):
    # cada processo só escreve no próprio tópico e na DLQ que alimenta, e só o
    # reservations lê orders.events. A autorização liga por último, com as ACLs já
    # gravadas; o superusuário vazio é o principal do listener externo. Criar
    # usuário e tópico tolera o "já existe", mas a presença de cada um é conferida
    # antes das ACLs: qualquer falha interrompe o init sem ligar a autorização.
    command:
      - |
        set -eu
        admin="-X admin.hosts=redpanda:9644"
        su="-X brokers=redpanda:29092 -X user=console -X pass=${KAFKA_CONSOLE_PASSWORD:-console-local} -X sasl.mechanism=SCRAM-SHA-256"
        for user in orders:${KAFKA_ORDERS_PASSWORD:-orders-local} \
          reservations:${KAFKA_RESERVATIONS_PASSWORD:-reservations-local} \
          bookings:${KAFKA_BOOKINGS_PASSWORD:-bookings-local} \
          console:${KAFKA_CONSOLE_PASSWORD:-console-local}; do
          rpk security user create "$${user%%:*}" -p "$${user#*:}" --mechanism SCRAM-SHA-256 $$admin || true
        done
        for user in orders reservations bookings console; do
          rpk security user list $$admin | grep -qw "$$user"
        done
        rpk cluster config set superusers '["console",""]' $$admin
        rpk topic create ${KAFKA_ORDERS_TOPIC:-orders.events} ${KAFKA_ORDERS_DLQ:-orders.events.dlq} \
          ${KAFKA_RESERVATIONS_TOPIC:-reservations.events} ${KAFKA_RESERVATIONS_DLQ:-reservations.events.dlq} \
          ${KAFKA_BOOKINGS_TOPIC:-bookings.events} ${KAFKA_BOOKINGS_DLQ:-bookings.events.dlq} \
          $$su -p 1 -r 1 || true
        for topic in ${KAFKA_ORDERS_TOPIC:-orders.events} ${KAFKA_ORDERS_DLQ:-orders.events.dlq} \
          ${KAFKA_RESERVATIONS_TOPIC:-reservations.events} ${KAFKA_RESERVATIONS_DLQ:-reservations.events.dlq} \
          ${KAFKA_BOOKINGS_TOPIC:-bookings.events} ${KAFKA_BOOKINGS_DLQ:-bookings.events.dlq}; do
          rpk topic describe "$$topic" $$su >/dev/null
        done
        rpk security acl create --allow-principal User:orders --operation write,describe \
          --topic ${KAFKA_ORDERS_TOPIC:-orders.events} --topic ${KAFKA_ORDERS_DLQ:-orders.events.dlq} $$su
        rpk security acl create --allow-principal User:reservations --operation write,describe \
          --topic ${KAFKA_RESERVATIONS_TOPIC:-reservations.events} --topic ${KAFKA_RESERVATIONS_DLQ:-reservations.events.dlq} \
          --topic ${KAFKA_ORDERS_DLQ:-orders.events.dlq} $$su
        rpk security acl create --allow-principal User:reservations --operation read,describe \
          --topic ${KAFKA_ORDERS_TOPIC:-orders.events} --group ${KAFKA_GROUP:-reservations} $$su
        rpk security acl create --allow-principal User:bookings --operation write,describe \
          --topic ${KAFKA_BOOKINGS_TOPIC:-bookings.events} --topic ${KAFKA_BOOKINGS_DLQ:-bookings.events.dlq} $$su
        rpk cluster config set kafka_enable_authorization true $$admin
    deploy:
      resources:
        limits: { cpus: '0.05', memory: 64M }
        reservations: { cpus: '0.01', memory: 16M }

  dmpf-orders-api:
    <<: [*app, *orders-image]
    command: ['--role', 'api']
    environment:
      <<: [*dmpf-env, *orders-env, *grpc-server-env]
      INSTANCE_ID: orders-api
      ITEM_LIMIT: ${ITEM_LIMIT:-10}
      GRPC_TLS_CERT_FILE: /pki/dmpf-orders-api.crt
      GRPC_TLS_KEY_FILE: /pki/dmpf-orders-api.key
    volumes: *pki
    depends_on:
      pki-init:
        condition: service_completed_successfully
      postgres-init:
        condition: service_completed_successfully
      otel-collector:
        condition: service_started
    deploy: *app-limits

  dmpf-orders-relay:
    <<: [*app, *orders-image]
    command: ['--role', 'relay']
    environment:
      <<: [*dmpf-env, *orders-env, *kafka-env, *kafka-orders]
      INSTANCE_ID: orders-relay
      KAFKA_ORDERS_TOPIC: ${KAFKA_ORDERS_TOPIC:-orders.events}
      KAFKA_ORDERS_DLQ: ${KAFKA_ORDERS_DLQ:-orders.events.dlq}
    depends_on:
      redpanda-init:
        condition: service_completed_successfully
      dmpf-orders-api:
        condition: service_started
    deploy: *worker-limits

  dmpf-reservations-api:
    <<: [*app, *reservations-image]
    command: ['--role', 'api']
    environment:
      <<: [*dmpf-env, *reservations-env, *grpc-server-env]
      INSTANCE_ID: reservations-api
      GRPC_TLS_CERT_FILE: /pki/dmpf-reservations-api.crt
      GRPC_TLS_KEY_FILE: /pki/dmpf-reservations-api.key
    volumes: *pki
    depends_on:
      pki-init:
        condition: service_completed_successfully
      postgres-init:
        condition: service_completed_successfully
      otel-collector:
        condition: service_started
    deploy: *app-limits

  dmpf-reservations-relay:
    <<: [*app, *reservations-image]
    command: ['--role', 'relay']
    environment:
      <<: [*dmpf-env, *reservations-env, *kafka-env, *kafka-reservations]
      INSTANCE_ID: reservations-relay
      KAFKA_RESERVATIONS_TOPIC: ${KAFKA_RESERVATIONS_TOPIC:-reservations.events}
      KAFKA_RESERVATIONS_DLQ: ${KAFKA_RESERVATIONS_DLQ:-reservations.events.dlq}
    depends_on:
      redpanda-init:
        condition: service_completed_successfully
      dmpf-reservations-api:
        condition: service_started
    deploy: *worker-limits

  dmpf-reservations-consumer:
    <<: [*app, *reservations-image]
    command: ['--role', 'consumer']
    environment:
      <<: [*dmpf-env, *reservations-env, *kafka-env, *kafka-reservations]
      INSTANCE_ID: reservations-consumer
      KAFKA_ORDERS_TOPIC: ${KAFKA_ORDERS_TOPIC:-orders.events}
      KAFKA_ORDERS_DLQ: ${KAFKA_ORDERS_DLQ:-orders.events.dlq}
    depends_on:
      redpanda-init:
        condition: service_completed_successfully
      dmpf-reservations-api:
        condition: service_started
    deploy: *worker-limits

  dmpf-bookings-api:
    <<: [*app, *bookings-image]
    command: ['--role', 'api']
    environment:
      <<: [*dmpf-env, *bookings-env, *grpc-server-env]
      INSTANCE_ID: bookings-api
      GRPC_TLS_CERT_FILE: /pki/dmpf-bookings-api.crt
      GRPC_TLS_KEY_FILE: /pki/dmpf-bookings-api.key
    volumes: *pki
    depends_on:
      pki-init:
        condition: service_completed_successfully
      postgres-init:
        condition: service_completed_successfully
      otel-collector:
        condition: service_started
    deploy: *app-limits

  dmpf-bookings-relay:
    <<: [*app, *bookings-image]
    command: ['--role', 'relay']
    environment:
      <<: [*dmpf-env, *bookings-env, *kafka-env, *kafka-bookings]
      INSTANCE_ID: bookings-relay
      KAFKA_GROUP: bookings
      KAFKA_BOOKINGS_TOPIC: ${KAFKA_BOOKINGS_TOPIC:-bookings.events}
      KAFKA_BOOKINGS_DLQ: ${KAFKA_BOOKINGS_DLQ:-bookings.events.dlq}
    depends_on:
      redpanda-init:
        condition: service_completed_successfully
      dmpf-bookings-api:
        condition: service_started
    deploy: *worker-limits

  dmpf-bff:
    <<: *app
    build:
      context: ../../..
      dockerfile: apps/backend/bff/Dockerfile
    image: ${BFF_IMAGE:-bff:local}
    environment:
      <<: *dmpf-env
      SERVICE: bff
      INSTANCE_ID: bff
      HTTP_ADDR: ':8080'
      ORDERS_GRPC_TARGET: dns:///dmpf-orders-api:9090
      RESERVATIONS_GRPC_TARGET: dns:///dmpf-reservations-api:9090
      BOOKINGS_GRPC_TARGET: dns:///dmpf-bookings-api:9090
      # Autenticador de desenvolvimento (IDN-01): lê a identidade declarada no
      # próprio Bearer; fora do compose a partida exige OIDC_ISSUER.
      AUTH_DEV_MOCK: 'true'
      # Sem GRPC_SERVER_NAME: cada alvo é verificado pelo próprio host.
      GRPC_CA_FILE: /pki/ca.crt
      GRPC_CLIENT_CERT_FILE: /pki/bff-client.crt
      GRPC_CLIENT_KEY_FILE: /pki/bff-client.key
      OPENAPI_ORDERS_PATH: /contracts/openapi/orders/v1/openapi.yaml
      OPENAPI_RESERVATIONS_PATH: /contracts/openapi/reservations/v1/openapi.yaml
      OPENAPI_BOOKINGS_PATH: /contracts/openapi/bookings/v1/openapi.yaml
      # Só o Swagger UI local chama o BFF de outro origin; fora do
      # desenvolvimento a variável fica vazia e a borda é same-origin.
      CORS_ORIGINS: http://localhost:${SWAGGER_PORT:-8082},http://127.0.0.1:${SWAGGER_PORT:-8082}
    ports:
      - '${HTTP_PORT:-8080}:8080'
    volumes: *pki
    depends_on:
      pki-init:
        condition: service_completed_successfully
      dmpf-orders-api:
        condition: service_started
      dmpf-reservations-api:
        condition: service_started
      dmpf-bookings-api:
        condition: service_started
      otel-collector:
        condition: service_started
    deploy: *app-limits
