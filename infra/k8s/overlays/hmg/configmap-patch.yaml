apiVersion: v1
kind: ConfigMap
metadata:
  name: dmpf-reference
data:
  # Kafka gerenciado com TLS (o default da app já é TLS ≥ 1.2; fica explícito).
  DMPF_KAFKA_INSECURE: 'false'
  DMPF_SERVICE_VERSION: hmg
