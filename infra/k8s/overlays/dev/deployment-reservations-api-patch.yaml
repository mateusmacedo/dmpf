apiVersion: apps/v1
kind: Deployment
metadata:
  name: reservations-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            # Em dev o api aplica o schema na partida (postgres.Migrate é
            # idempotente); relay e consumer reiniciam até as tabelas existirem.
            - name: MIGRATE
              value: 'true'
