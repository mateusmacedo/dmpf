apiVersion: v1
kind: Service
metadata:
  name: reservations-api
  labels:
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: api
  ports:
    - name: grpc
      port: 9090
      targetPort: grpc
      appProtocol: grpc
