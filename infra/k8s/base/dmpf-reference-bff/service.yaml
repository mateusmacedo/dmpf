apiVersion: v1
kind: Service
metadata:
  name: dmpf-reference-bff
  labels:
    app.kubernetes.io/component: bff
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: bff
  ports:
    - name: http
      port: 8080
      targetPort: http
