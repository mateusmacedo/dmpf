# O relay drena por claim com lease (OBX-09) só a outbox do banco
# dmpf_reservations, onde o consumer enfileira ReservationConfirmed.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dmpf-reference-reservations-relay
  labels:
    app.kubernetes.io/component: relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: relay
  template:
    metadata:
      labels:
        app.kubernetes.io/component: relay
    spec:
      serviceAccountName: dmpf-reference-reservations
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: relay
          image: ghcr.io/mateusmacedo/dmpf-reference-reservations:latest
          args: ['--role', 'relay']
          envFrom:
            - configMapRef:
                name: dmpf-reference-reservations
            - secretRef:
                name: dmpf-reference-reservations
          env:
            - name: DMPF_INSTANCE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
      # ShutdownGrace do relay é 10 s; o pod espera mais que isso.
      terminationGracePeriodSeconds: 20
