syntax = "proto3";

package company.reservations.event.v1;

// A reservation was confirmed in the reservations bounded context.
// Envelope type: com.company.reservations.reservation-confirmed.v1 (PTB-03).
message ReservationConfirmed {
  string order_id = 1;
  int32 item_count = 2;
}
