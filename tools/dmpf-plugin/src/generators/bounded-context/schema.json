{
  "$schema": "https://json-schema.org/schema",
  "$id": "DmpfBoundedContext",
  "title": "Gera um bounded context DMPF em um módulo Go, um package por bloco",
  "description": "Gera um bounded context DMPF como um único módulo Go — um package por bloco —, com manifesto declarado e entrada no go.work. contract fica fora (contracts/ pelo rito Buf).",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nome do contexto em kebab-case; diretório do módulo (<directory>/<name>), nome do projeto Nx e do pacote npm privado. Os packages Go levam o nome do bloco (domain, ports, application, provider, app).",
      "pattern": "^[a-z][a-z0-9-]*$",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Qual o nome do contexto (kebab-case)?"
    },
    "boundedContext": {
      "type": "string",
      "description": "Valor literal de bounded_context nos manifestos dmpf-units.json. Declarado, nunca inferido do nome nem do diretório (ADR-012).",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "blocks": {
      "type": "array",
      "description": "Blocos DMPF a gerar, cada um como package do módulo. contract fica fora (fonte em contracts/ pelo rito Buf); um subconjunto precisa fechar as dependências entre blocos.",
      "items": {
        "type": "string"
      },
      "default": ["domain", "port", "application", "provider", "app"]
    },
    "directory": {
      "type": "string",
      "description": "Diretório que recebe o módulo do contexto, relativo à raiz do workspace.",
      "default": "apps/backend"
    },
    "serviceName": {
      "type": "string",
      "description": "Nome qualificado do serviço gRPC do contexto (package proto mais o serviço). Sem a opção, vira company.<name sem hífens>.service.v1.<Name>Service.",
      "pattern": "^[a-z][a-z0-9]*(\\.[a-z][a-z0-9]*)*\\.[A-Z][A-Za-z0-9]*$"
    }
  },
  "required": ["name", "boundedContext"]
}
