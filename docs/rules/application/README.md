# Regras de Aplicação (APP)

Regras técnicas e de qualidade de código.

Formato: `REGRA APP-NNN`, com a estrutura definida em
[Estrutura de uma regra](../README.md#estrutura-de-uma-regra).

## Regras

*Nenhuma regra registrada ainda.*

<!-- Exemplo:
### REGRA APP-001: Validação de input em endpoints públicos

**Contexto**: Aplica-se a todo endpoint exposto fora da rede interna.

**Regra**: Todo endpoint público valida o input com schema antes de usá-lo, e
confia apenas nos dados já validados no servidor.

**Exemplos**:
- Caso válido: o handler recebe o resultado do parse do schema
- Caso inválido: o handler lê `req.body` direto e confia no formato

**Exceções**: Endpoints internos atrás de gateway autenticado podem validar
apenas os campos que consomem.

**Origem**: Incidente de segurança, 2025-02-10
-->
